node_modules/
dist/

# All .env variants — .env.local, .env.production, etc. — except the example
.env*
!.env.example

# All data directory contents (tokens, etc.) — keep only .gitkeep
data/*
!data/.gitkeep

# Private keys and certs
*.pem
*.key
*.p12
*.pfx
